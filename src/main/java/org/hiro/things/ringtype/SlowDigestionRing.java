package org.hiro.things.ringtype;

import org.hiro.things.Ring;
import org.hiro.things.RingEnum;

public class SlowDigestionRing extends Ring {
    public SlowDigestionRing(){
        super();
        this._o_which = RingEnum.SlowDigestion.getValue();
    }
}
